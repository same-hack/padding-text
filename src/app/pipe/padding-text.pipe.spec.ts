import { PaddingTextPipe } from './padding-text.pipe';

describe('PaddingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PaddingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
